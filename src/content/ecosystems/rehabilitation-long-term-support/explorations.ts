import { plansToExplorations } from '../../seed-builders';
import { rehabilitationLongTermSupportPlans } from './exploration-plans';

export const rehabilitationLongTermSupportExplorations =
  plansToExplorations(rehabilitationLongTermSupportPlans);
