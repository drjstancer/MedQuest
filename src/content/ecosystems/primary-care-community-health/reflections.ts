import { plansToReflections } from '../../seed-builders';
import { primaryCareCommunityHealthPlans } from './exploration-plans';

export const primaryCareCommunityHealthReflections =
  plansToReflections(primaryCareCommunityHealthPlans);
