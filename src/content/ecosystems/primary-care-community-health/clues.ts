import { plansToClues } from '../../seed-builders';
import { primaryCareCommunityHealthPlans } from './exploration-plans';

export const primaryCareCommunityHealthClues =
  plansToClues(primaryCareCommunityHealthPlans);
