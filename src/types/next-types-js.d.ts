declare module 'next/types.js' {
  export type {
    Metadata,
    MetadataRoute,
    ResolvedMetadata,
    ResolvedViewport,
    ResolvingMetadata,
    ResolvingViewport,
    Viewport,
  } from 'next/dist/types';
}

declare module 'next' {
  export type {
    Metadata,
    MetadataRoute,
    NextConfig,
    ResolvedMetadata,
    ResolvedViewport,
    ResolvingMetadata,
    ResolvingViewport,
    Viewport,
  } from 'next/dist/types';
}
